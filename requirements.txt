# Runtime dependencies for the FH6 Tracker GUI.
# tkinter and the standard library cover the core app; these add the optional
# on-screen credit OCR (Settings -> Automatic Credit Tracking).
pyautogui>=0.9.54
pytesseract>=0.3.10
Pillow>=10.0.0
