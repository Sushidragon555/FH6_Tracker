__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# First-run marker created by the launcher
.deps_installed

# Runtime data / generated files (grow at runtime; not part of the app)
telemetry_log.csv
fh6_tracker.log
*.tmp

# PyInstaller build output
build/
dist/
*.spec

# OCR debug captures (generated at runtime when debug logging is enabled)
ocr_debug/
ocr_debug_capture.png

# Race telemetry data (generated per user)
races/

# User-specific data (generated on first run, not shared)
gui_settings.json
session_state.json
credit_transactions.json
credit_history.json
methods_history.json
owned_cars.json
.last_update
exports/
