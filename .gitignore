__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# First-run marker created by the launcher
.deps_installed

# Runtime data / generated files (grow at runtime; not part of the app)
telemetry_log.csv
fh6_tracker.log
*.tmp

# PyInstaller build output
build/
dist/
*.spec

# OCR debug captures (generated at runtime when debug logging is enabled)
ocr_debug/
ocr_debug_capture.png
